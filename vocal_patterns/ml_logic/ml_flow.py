# For running and transitioning models via MlFlow

import mlflow
import mlflow.keras
import mlflow.tensorflow


def mlflow_transition_model(current_stage: str, new_stage: str):
    pass


def mlflow_run(func):
    pass
